@model string

@{ var watermark = ViewData.ModelMetadata.AdditionalValues.ContainsKey("watermark") ? R(ViewData.ModelMetadata.AdditionalValues["watermark"].ToString()) : null; }

@Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue,
    new
    {
        @class = "form-control",
        placeholder = watermark

    })
